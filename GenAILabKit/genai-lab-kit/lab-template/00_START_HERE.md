# Start Here

Welcome. You will build **<what>** in about <N> minutes.

## 1. What you are building

<2-3 sentences in plain language.>

## 2. Your environment is ready

This VM already has Python, Jupyter, VS Code, every package, and all secrets
configured. **Do not run `pip install` or create API keys.**

## 3. What to do

1. Run the preflight check:
   ```bash
   python scripts/setup_check.py
   ```
   Expect `ALL CHECKS PASSED`.

2. Open the participant notebook:
   ```bash
   cd 02_STARTER_CODE
   jupyter notebook participant_notebook.ipynb
   ```

3. Run **Task 0**, then complete the tasks in order using
   [`01_TASK_GUIDE/TASKS.md`](01_TASK_GUIDE/TASKS.md).

4. Finish with:
   ```bash
   python scripts/validate_solution.py
   ```

## 4. Rules of the lab

- <data / setting constraints>

## 5. If you get stuck

Each task has progressive hints. The full solution is released by your instructor
after the session.
