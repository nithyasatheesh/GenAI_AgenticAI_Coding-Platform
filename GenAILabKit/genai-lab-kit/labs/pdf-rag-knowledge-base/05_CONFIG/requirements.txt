# Preinstall ALL of these on the VM before participant access.
# Pinned minor versions keep every participant environment identical.
# Verify with:  python scripts/setup_check.py

langchain>=0.3,<0.4
langchain-openai>=0.2,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langchain-chroma>=0.1,<0.3
chromadb>=0.5,<0.7
pypdf>=4,<6
python-dotenv>=1,<2

# IDE / notebook runtime
jupyter>=1,<2
notebook>=7,<8
ipykernel>=6,<7

# used by other labs on the same VM image
streamlit>=1.36,<2
