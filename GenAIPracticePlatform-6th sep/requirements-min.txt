# ABSOLUTE FLOOR — only if requirements.txt itself still won't build (it should).
# Drops the LLM SDKs too: Mock mode + Lab 1 (RAG) + the graded UI still work;
# OpenAI / Claude real modes will say "add langchain-openai" on Run.
streamlit
pyyaml
openpyxl
langchain-core
langchain-community
langchain-text-splitters
pypdf
