# FULL stack — every capability: ChromaDB, LangGraph agents, MCP, keyless
# FastEmbed embeddings. Pulls native wheels (onnxruntime, tokenizers, ormsgpack)
# that are fine on a normal machine but exceed Streamlit COMMUNITY Cloud's
# free-tier build limit. Use this locally / on a paid tier / in a container:
#
#   pip install -r requirements-full.txt
#   streamlit run streamlit_app.py
#
# Install under Python 3.12 so every native package installs from a prebuilt
# wheel with no compiler. (Set the version in your host's config / Streamlit
# Advanced settings — NOT a runtime.txt file, which Streamlit Cloud mis-handles.)

-r requirements.txt

# --- vector DB (real RAG store) ---
chromadb
langchain-chroma

# --- agent / multi-agent workflows (labs 03, 04) ---
langgraph

# --- MCP lab (05) ---
mcp
langchain-mcp-adapters

# --- keyless local embeddings (real ONNX model, no API key) ---
fastembed
