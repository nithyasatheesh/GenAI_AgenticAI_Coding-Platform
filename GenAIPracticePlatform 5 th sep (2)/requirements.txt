# Default deploy set. Pure-wheel only — no chromadb / fastembed / onnxruntime /
# rust crates — so Streamlit Community Cloud's `uv` installer builds it in ~1 min.
# Verified: `uv pip compile` resolves 91 packages on Python 3.11 / 3.12 / 3.13.
# The heavy extras (ChromaDB, FastEmbed, MCP) live in requirements-full.txt.

streamlit>=1.5,<2
pyyaml>=6,<7
openpyxl>=3,<4
langchain-core>=1,<2
langchain-community>=0.4,<1
langchain-text-splitters>=1,<2
langgraph>=1,<2
langchain-openai>=1,<2
langchain-anthropic>=1,<2
pypdf>=6,<7
