# FULL stack — the default requirements.txt PLUS real ChromaDB, keyless FastEmbed
# embeddings, and the MCP lab. These pull native/compiled deps (onnxruntime,
# chroma-hnswlib, rust crates) that DON'T reliably build on Streamlit Community
# Cloud, so keep them out of the default deploy. Use this file:
#   * locally / in a venv:   pip install -r requirements-full.txt
#   * in Docker or a VM with build tools
#   * on a Cloud tier that provides a compiler
#
# Verified: fresh-venv install on Python 3.13 -> 144 packages, exit 0.

-r requirements.txt

# real vector DB
chromadb>=1,<2
langchain-chroma>=1,<2

# real local embeddings, no API key (downloads a small ONNX model on first use)
fastembed>=0.7,<1

# the MCP lab (Lab 5)
mcp>=1.9,<2
langchain-mcp-adapters>=0.3,<1
