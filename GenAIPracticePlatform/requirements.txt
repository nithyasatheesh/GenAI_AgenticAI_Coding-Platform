# Minimal: only what the app, sandbox.py, and the Run-button exercise code import.
# Unpinned so the deployment Python (3.11 / 3.12 / 3.13) gets compatible wheels.
# All pure-Python except pyarrow, which is streamlit's own (always wheeled).
#
# Deliberately excluded:
#   langchain            -> meta package, nothing imports `langchain`
#   chromadb / langchain-chroma -> unreliable native build on Streamlit Cloud;
#                                  sandbox uses langchain-core InMemoryVectorStore
#   langchain-openai / langchain-anthropic -> real-LLM modes only (see bottom)

streamlit
pyyaml
langchain-core
langchain-community
langchain-text-splitters
langgraph
pypdf

# Real-LLM modes (optional). Mock mode needs neither. Uncomment ONE (or both),
# redeploy, then add the matching key in Streamlit -> Settings -> Secrets:
#   OpenAI mode -> langchain-openai     + OPENAI_API_KEY
#   Claude mode -> langchain-anthropic  + ANTHROPIC_API_KEY   (also set ANTHROPIC_MODEL
#                  if your account's model id differs from the default)
# langchain-openai
# langchain-anthropic
