# FALLBACK — the smallest set that boots the app and runs Lab 1 (RAG) + the full
# graded UI. Every package here is long-established with wheels for every Python.
# Use this if `requirements.txt` fails to build on Streamlit Cloud:
# rename this file to requirements.txt (or point the app at it), redeploy.
#
# With this set, Labs 2-5 show "add langgraph / langchain-openai to requirements"
# when you press Run — add those lines back once the base deploy is green.

streamlit
pyyaml
langchain-core
langchain-community
langchain-text-splitters
pypdf
openpyxl
